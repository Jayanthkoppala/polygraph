/**
 * HowItWorks — DELETED per positioning.md §3 ("Deleted sections:
 * TaglineReveal, Benefits, HowItWorks, FleetScale, PipelineFlowchart") and
 * copy.md §2 ("write no copy for these"). How-it-works is now three plain
 * lines inside the hero (S1), where it cannot compete with the sandbox;
 * the four checks are explained in-app (copy.md §3.7) and the decision
 * vocabulary lives in the verdict chips themselves.
 *
 * Stub, not a file deletion: `LandingPage.tsx` (the hero agent's file,
 * outside this task's write scope) still imports it. Remove that import
 * and this file together.
 */
export function HowItWorks() {
  return null;
}

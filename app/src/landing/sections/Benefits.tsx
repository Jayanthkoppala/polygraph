/**
 * Benefits — DELETED per positioning.md §3 ("Deleted sections:
 * TaglineReveal, Benefits, HowItWorks, FleetScale, PipelineFlowchart. The
 * hero covers what they covered or they covered nothing a stranger
 * needed") and copy.md §2 ("write no copy for these"). Its custody facts
 * moved to S4 (Receipt.tsx); its who-it's-for framing is carried by the
 * hero and S2.
 *
 * Stub, not a file deletion: `LandingPage.tsx` (the hero agent's file,
 * outside this task's write scope) still imports it. Remove that import
 * and this file together.
 */
export function Benefits() {
  return null;
}

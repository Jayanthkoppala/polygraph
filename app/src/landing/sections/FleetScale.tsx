/**
 * FleetScale — DELETED per positioning.md §3 ("Deleted sections: ...
 * FleetScale ...") and §4's veto on WebGL/`Threads`-style canvases
 * competing for attention. Its test suite (FleetScale.test.tsx) was
 * removed with it — the four §3.9 gates it asserted have no subject once
 * the canvas is gone.
 *
 * Stub, not a file deletion: `LandingPage.tsx` (the hero agent's file,
 * outside this task's write scope) still imports it. Remove that import,
 * this file, `src/landing/webgl/StaticThreads.tsx`, and
 * `src/components/Threads.tsx` together (the latter two are outside this
 * task's scope).
 */
export function FleetScale() {
  return null;
}
